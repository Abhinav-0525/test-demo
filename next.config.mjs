/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        port: '',
        pathname: '/**',
      },
    ],
    // Alternative configuration for Next.js versions that support it
    domains: ['images.unsplash.com'],
  },
}

export default nextConfig